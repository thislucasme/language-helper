import axios from "axios";

export class ChatRepository {
  private readonly API_URL = "https://api.openai.com";

  public async sendMessage(message: string): Promise<any> {
    // Implementar lógica para enviar mensagem para a API
  }
}
